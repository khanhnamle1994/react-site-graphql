image: codogo/pipelines-yarn:latest

pipelines:
  default:
    - step:
        script:
          - yarn install
          - yarn run lint 

  tags:
    v*: 
      - step:
          script:
            - yarn install
            - yarn run lint
            - mv .npmrc_config .npmrc
            - npm publish
